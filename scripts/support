#!/usr/bin/env bash
set -euo pipefail

if [ -f /.venv/bin/activate ]; then
    # shellcheck disable=SC1091
    source /.venv/bin/activate
fi
support_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec python3 "$support_dir/support.py" "$@"
